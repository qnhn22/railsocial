<%= form_with(model: [post, post.comments.build], method: :post) do |form| %>
  <%= form.text_area :body, placeholder: "Write your comment here"%>
  <button type="submit">Create</button>
  <span><%= post.comments.count %></span>
<% end %>