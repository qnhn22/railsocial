<div>
    <% if post.likes.count > 0 %>
        <button type="button">
            <%= post.likes.count %> Likes
        </button>
            <div>
                <ul class="list-group">
                    <% post.likes.order(created_at: :desc).each do |like| %>
                            <%= like.user.email %>
                    <% end %>
                </ul>
            </div>
    <% end %>
</div>